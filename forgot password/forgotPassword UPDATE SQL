####################################
# forgot password validation	   #
####################################
def forgot_passwords():
	sqlUpdate = "UPDATE Customer_Table SET password = "+ "'"+ str(reEnter_password_Box.value)+"'"+ " WHERE username = "+ "'" + str(Username_box.value)+"'"
	execute_sql(database_file, sqlUpdate)
	info("Congrats", "Your password has been updated!")
	close_forgot_window()
